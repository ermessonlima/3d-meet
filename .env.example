# Copie para .env e preencha. O .env nao vai para o git.
# A chave fica so no processo do servidor; o navegador nunca a ve.
OPENAI_API_KEY=

# Opcional: troque o modelo sem mexer no codigo.
# OPENAI_MODEL=gpt-4o-mini
